# ============================================
# WORKFLOW ĐƠN GIẢN NHẤT - DEMO CHO HỌC SINH
# ============================================

name: Demo CI

# Khi nào chạy?
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Danh sách jobs
jobs:
  hello:
    name: 👋 Chào mừng
    runs-on: ubuntu-latest

    steps:
      - name: 🎉 In ra thông báo
        run: |
          echo "=========================================="
          echo "🚀 Workflow đã chạy thành công!"
          echo "=========================================="
          echo ""
          echo "📅 Thời gian: $(date)"
          echo "👤 Người push: ${{ github.actor }}"
          echo "� Repository: ${{ github.repository }}"
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "📝 Commit: ${{ github.sha }}"
          echo ""
          echo "=========================================="
      - name: Kiểm tra thư mục hiện tại
        run: ls -la