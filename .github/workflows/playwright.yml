# ============================================
# WORKFLOW DEMO - GITHUB ENVIRONMENTS
# Dùng file TypeScript để in secrets/vars
# ============================================

name: Demo Environments

on:
  push:
    branches: [ 'feature/test-dispatch' ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Chọn môi trường'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - uat

jobs:
  test:
    name: 🧪 Test trên ${{ inputs.environment }}
    runs-on: ubuntu-latest
    
    # 📌 SỬ DỤNG ENVIRONMENT
    environment: ${{ inputs.environment }}

    steps:
      - name: � Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: � Install dependencies
        run: npm ci

      - name: � Chạy print-env.ts
        run: npx tsx tests/lessons/print-env.ts
        env:
          # Truyền secrets/vars từ environment vào biến môi trường
          MY_PASSWORD: ${{ secrets.MY_PASSWORD }}
          MY_WEBSITE: ${{ vars.MY_WEBSITE }}
          ENVIRONMENT: ${{ inputs.environment }}